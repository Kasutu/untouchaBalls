import Canvas from './canvas.ux.js';
const { clear, context, drawBox, drawImage, drawInvisibleHitBox, height, width, } = new Canvas();
export { clear, context, drawBox, drawImage, drawInvisibleHitBox, height, width, };
